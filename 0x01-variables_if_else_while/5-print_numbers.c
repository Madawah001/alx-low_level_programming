#include <stdio.h>
/**
 * main - Prints all single digit numbers
 * Return: Always (Success)
 *
 */
int main(void)

{
	int l;

	for (l = 0; l < 10; l++)
	printf("%d", l);
	printf("\n");

	return (0);
}
